"use client";
import React from "react";

const layout = ({ children }) => {
  // console.log("inside pages/layout", children);
  return <div className="">{children}</div>;
};

export default layout;
